import { Chess } from 'chess.js';

function createGame() {
    // Creates the game and return a gameid (Where to connect to)
    const chess = new Chess();
    return chess
}

function validateMove() {
    
}